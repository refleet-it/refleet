; Never output PHP errors/warnings to the HTTP response in production.
; This prevents HTML contamination in JSON API responses (e.g. post_max_size exceeded warning).
display_errors = Off
display_startup_errors = Off
log_errors = On
