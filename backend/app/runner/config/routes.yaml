runner:
    resource: ../src/Runner/Infrastructure/Api/
    type: attribute
    prefix: /api
    defaults:
        _format: json

# Container-to-container only, gated by the `internal` firewall — see
# App\Shared\Infrastructure\Http\InternalApiClient and docs/adr/0001-multiple-kernels.md.
runner_internal:
    resource: ../src/Runner/Infrastructure/Api/Internal/
    type: attribute
    prefix: /internal
    defaults:
        _format: json
