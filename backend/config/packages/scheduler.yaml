# Recurring in-app jobs. Each schedule is declared with #[AsSchedule] in the context that
# owns it - App\Shift\Shift\Infrastructure\Scheduler\ShiftSchedule is the first - and reaches
# that context's worker as an extra transport on its messenger:consume (see compose.yml).
framework:
    scheduler:
        enabled: true
